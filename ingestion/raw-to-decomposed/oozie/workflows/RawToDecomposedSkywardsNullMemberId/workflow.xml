<workflow-app name="RawToDecomposedSkywards_HEI8842_Naveen" xmlns="uri:oozie:workflow:0.5">
  <global>
            <configuration>
                <property>
                    <name>oozie.launcher.mapred.job.queue.name</name>
                    <value>model</value>
                </property>
                <property>
                    <name>mapred.job.queue.name</name>
                    <value>model</value>
                </property>
            </configuration>
  </global>
    <start to="decision-11f3"/>
    <kill name="Kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <decision name="decision-11f3">
        <switch>
            <case to="shell-f6a4">
              ${ 1 gt 0 }
            </case>
            <case to="shell-4b21">
              ${ 1 gt 0 }
            </case>
            <default to="End"/>
        </switch>
    </decision>
    <action name="shell-f6a4">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>read_avro_schema.sh</exec>
              <argument>${TARGET_SCHEMA_SDW_CUSTOMER}</argument>
            <file>read_avro_schema.sh#read_avro_schema.sh</file>
              <capture-output/>
        </shell>
        <ok to="pig-4692"/>
        <error to="email-efcd"/>
    </action>
    <action name="pig-4692">
        <pig>
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <prepare>
                  <delete path="${nameNode}${TARGET_DIRECTORY_SDW_CUSTOMER}"/>
            </prepare>
            <script>RawToDecomposed.pig</script>
              <param>SOURCE_DIRECTORY=${SOURCE_DIRECTORY_SDW_CUSTOMER}</param>
              <param>SOURCE_SCHEMA_FILE=${SOURCE_SCHEMA_SDW_CUSTOMER}</param>
              <param>TARGET_DIRECTORY=${TARGET_DIRECTORY_SDW_CUSTOMER}</param>
              <param>TARGET_SCHEMA=${wf:actionData(&#39;shell-f6a4&#39;)[&#39;AVRO_SCHEMA&#39;]}</param>
            <file>RawToDecomposed.pig#RawToDecomposed.pig</file>
            <file>pigudf.jar#pigudf.jar</file>
        </pig>
        <ok to="End"/>
        <error to="email-efcd"/>
    </action>
    <action name="shell-4b21">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${jobTracker}</job-tracker>
            <name-node>${nameNode}</name-node>
            <exec>helix_donothing.sh</exec>
            <file>helix_donothing.sh#helix_donothing.sh</file>
              <capture-output/>
        </shell>
        <ok to="End"/>
        <error to="email-efcd"/>
    </action>
    <action name="email-efcd">
        <email xmlns="uri:oozie:email-action:0.2">
            <to>naveen.k@dnata.com</to>
            <subject>Workflow ${wf:name()} failed</subject>
            <body>Current timestamp: ${timestamp()}
Workflow name: ${wf:name()}
Workflow id: ${wf:id()}
Workflow application path: ${wf:appPath()}
Workflow user: ${wf:user()}
Last error action: ${wf:lastErrorNode()}
Error code: ${wf:errorCode(wf:lastErrorNode())}
Error message: ${wf:errorMessage(wf:lastErrorNode())}</body>
            <content_type>text/plain</content_type>
        </email>
        <ok to="Kill"/>
        <error to="Kill"/>
    </action>
    <end name="End"/>
</workflow-app>